# New preparation checks — 11 September 2026

The anonymous reviewer derivative was checked using saved-evidence verification, source-to-identity/frozen-map raw WCL, descriptive fit geometry and the retained join fixtures. Core reconstruction passed 21 checks across 9,976 arm/case records. The geometry run passed 26 checks; 17 regenerated descriptor files matched saved records, ignoring gzip headers. These checks do not refit receiver coordinates, run the non-WCL optimizers, or calculate new bootstrap samples.

All 230 retained scientific source/data/population/environment/result files are byte-identical to the preserved master; scientific fitting and localization routines are not rewritten. Wrapper/document tasks and manifests are separately versioned. The geometry descriptor changes only its input-manifest binding and associated input receipt; all other parsed source statements are identical.

Seventeen full-precision numerical table views match the master's table generator byte-for-byte. Both documents preserve every Markdown table row (49 main and 275 supplementary) and all 7 main/8 supplementary displayed equation blocks. Current figures are reused, not recalculated from changed predictions.

The PDFs have 18 and 27 pages respectively. All pages were rendered for layout checks and compared to the attributed master. Twenty-nine pages are pixel-identical; changed pages received layout inspection, with individual inspection of the declaration/availability and changed command pages. All four Word documents were rendered and visually inspected outside this reviewer archive. Those Word documents contain either editorial metadata or an explicitly unconfirmed declaration draft and are not computational evidence.

Two intermediate supplement builds failed after attempted layout grouping of verbatim command blocks. The error was repaired using pre-block space reservation, and the final complete build succeeded; only the final complete PDF pair is included. Earlier failed builds are not counted as successful verification. A comparison checker initially matched the wrong mathematical delimiter and was corrected to require nonempty equation-block counts before declaring equality.

Preserved producer receipts retain their historical version labels and execution scopes. They are not described as newly performed fits. Local checks are not independent external replication, physical catalogue verification, journal receipt, public repository upload, or a new reuse-license grant.
